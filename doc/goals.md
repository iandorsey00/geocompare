# Goals

- Let users access demographic data locally without hand-editing source data files.
- Make common geographic questions easy to answer from a local CLI workflow.
- Support multiple summary levels cleanly, including states, counties, places, and ZCTAs.
- Expose both raw and derived metrics through stable query identifiers.
- Keep optional overlays practical, incremental, and local-first.
